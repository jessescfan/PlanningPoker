<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.initial.simpleplanningpoker">

    <application
        android:allowBackup="true"
        android:icon="@drawable/coffee_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/coffee_icon"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.initial.simpleplanningpoker.FullscreenDisplayNumberActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_fullscreen_display_number"
            android:screenOrientation="portrait"
            android:theme="@style/FullscreenTheme" />
        <activity android:name="com.initial.simpleplanningpoker.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service android:name="com.initial.simpleplanningpoker.ShakeService"></service>

    </application>

</manifest>